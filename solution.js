// Your code here

function reversed_string(str){
  let str_reversed = ""
  
  if (string is even) {
    // reverse an even word
    let length = lentgh of string;
    for (let i =0;  length/2;  i++;){
      // swap the ith and (length-1)-i 'th letters
    };
  } else{
    // reverse an odd word
    let length = lentgh of string -1;
    for (let i =0;  length/2;  i++;){
      // swap the ith and (length-1)-i 'th letters
    };
    
  }
  
  return str_reversed;
};

// Be sure it's the cleanest code you can write!
